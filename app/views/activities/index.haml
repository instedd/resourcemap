- content_for :head do
  :javascript
    $(function() { initActivities(); });

%h1 Activity

.tabsline
  .tablewrapp
    %table.GralTable
      %tr
        %th Collection
        %th User
        %th Activity
        %th Time
      <!-- ko foreach: activities -->
      %tr{style: 'height:32px'}
        %td{ko(text: :collection)}
        %td{ko(text: :user)}
        %td{ko(text: :description)}
        %td.timeago{ko(attr: {title: :createdAt}, text: :createdAt), style: 'min-width:150px'}
      <!-- /ko -->
      <!-- ko if: hasMore() -->
      %tr{style: 'height:32px'}
        %td{ko(click: :loadMore), colspan: 4, align: :center, style: 'text-decoration:underline;cursor:pointer'}
          Load more
      <!-- /ko -->
